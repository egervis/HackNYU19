"use strict";

// Import statements
import endpoints from 'src/endpointMapper';

const express = require('express');
const postgres = require('pg');
const app = express();
const port = 3000;

// Initial endpoint
app.get('/', );

app.listen(port);
